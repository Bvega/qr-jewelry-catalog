// Local static fallback. The Pages artifact replaces this file with a
// validated browser-safe URL and publishable key at build time.
window.BETWEEN_US_PUBLIC_CONFIG = Object.freeze({
  url: "",
  publishableKey: ""
});
