# BETWEEN US PLATFORM — IMPLEMENTATION PACKAGE

## Milestone M04 — Collections and Discovery

**Version:** 1.0  
**Status:** Approved for implementation candidate  
**Target repository:** `/Users/miniboli/DEV/qr-jewelry-catalog`  
**Target branch:** `feature/m04-collections-discovery`  
**MASTER source of truth:** `docs/MASTER_SPECIFICATION.md`  
**Required baseline:** M03 approved, merged, validated, and pushed at commit `34981ca` or a later `main` containing it  
**Implementation environment:** ChatGPT Work  
**Implementation model:** GPT-5.6 Sol, Extra High reasoning

---

# 1. ROLE

You are the implementation engine for the Between Us Platform.

The MASTER specification and this milestone package are authoritative.

Do not make product, architecture, route, framework, backend, data-history, or scope decisions outside this package.

Inspect first. Implement second. Validate third. Report last.

M04 adds the first real discovery behavior to the approved Between Us public shell. It must remain a static, dependency-free implementation and must preserve every accepted M01–M03 compatibility contract.

M04 is an implementation candidate requiring owner visual acceptance before merge.

---

# 2. PROJECT CONTEXT

The repository is now the public **Between Us Finds** catalog.

Accepted foundation:

## M01

- dependency-free repository validation;
- legacy URL protection;
- QR/share protection;
- static-serving tests;
- GitHub Actions validation.

## M02

- normalized `window.BETWEEN_US_FINDS`;
- derived `window.JEWELRY_ITEMS`;
- permanent public IDs `BU-0001` through `BU-0005`;
- stable slugs;
- lookup helpers;
- exact legacy adapter parity.

## M03

- approved Between Us identity;
- local SVG logo;
- approved palette and typography;
- Home, Collections, Explore, and About shell;
- responsive shared header and footer;
- Find public vocabulary;
- 63 passing tests;
- owner visual acceptance.

Current content:

- five Jewelry Finds;
- three real product photographs;
- two known missing-image records with working fallback;
- no inventory outside Jewelry yet.

M04 must build a complete collection/discovery system that works now with Jewelry and remains ready for future collections without inventing unavailable inventory.

---

# 3. MILESTONE OBJECTIVE

Implement a static, accessible, future-ready Collections and Discovery experience.

M04 must:

1. Establish one authoritative Collection registry.
2. Establish one authoritative editorial Discovery configuration.
3. Activate Jewelry as the current live Collection.
4. Preserve Vintage, Home & Decor, Kitchen, Collectibles, and New Items as honest Coming Soon Collections.
5. Add functional Collection filtering to Explore.
6. Add Featured Finds.
7. Add Latest Finds.
8. Add Find of the Week.
9. Reuse one consistent Find-card rendering system.
10. Add collection and discovery metadata without duplicating the Find catalog.
11. Preserve all current routes, IDs, QR/share behavior, data values, and item details.
12. Add automated collection/discovery contracts.
13. Add desktop and mobile visual validation.
14. Stop before push or merge for owner visual acceptance.

---

# 4. PRECONDITIONS

Before making changes:

1. Read:

```text
docs/MASTER_SPECIFICATION.md
docs/MILESTONES/M04.md
docs/BASELINE_BEHAVIOR.md
docs/DOMAIN_MODEL.md
docs/COMPATIBILITY_ADAPTER.md
docs/IDENTIFIER_REGISTRY.md
docs/BRAND_SYSTEM.md
docs/PUBLIC_SHELL.md
docs/REPORTS/M01_REPORT.md
docs/REPORTS/M02_REPORT.md
docs/REPORTS/M03_REPORT.md
docs/VALIDATION.md
index.html
item.html
app.js
item.js
styles.css
data/items.js
scripts/validate-baseline.mjs
all current tests
```

2. Run:

```bash
git status --short
git branch --show-current
git fetch origin
git rev-parse main
git rev-parse origin/main
git merge-base --is-ancestor 34981ca main
node scripts/validate-baseline.mjs
```

3. Confirm:

- the working tree is clean;
- the current branch is `main`;
- `main` equals `origin/main`;
- commit `34981ca` is an ancestor of `main`;
- all 63 existing tests pass.

4. If any precondition fails, stop and report.

5. Create:

```bash
git switch -c feature/m04-collections-discovery
```

---

# 5. APPROVED COLLECTION REGISTRY

Create one authoritative static Collection registry.

Preferred file:

```text
data/collections.js
```

Expose:

```text
window.BETWEEN_US_COLLECTIONS
```

The registry must contain exactly these six initial Collections in this order:

| ID | Public label | Status | Active now |
|---|---|---|---|
| `jewelry` | Jewelry | `active` | Yes |
| `vintage` | Vintage | `coming-soon` | No |
| `home-decor` | Home & Decor | `coming-soon` | No |
| `kitchen` | Kitchen | `coming-soon` | No |
| `collectibles` | Collectibles | `coming-soon` | No |
| `new-items` | New Items | `coming-soon` | No |

Each Collection record must support:

```text
id
label
description
status
sortOrder
```

Approved descriptions:

## Jewelry

```text
Wearable finds selected for style, detail, and everyday discovery.
```

## Vintage

```text
Distinctive pieces with character, history, and lasting appeal.
```

## Home & Decor

```text
Useful and decorative finds for comfortable, personal spaces.
```

## Kitchen

```text
Practical kitchen finds chosen for everyday use.
```

## Collectibles

```text
Interesting pieces worth noticing, keeping, or sharing.
```

## New Items

```text
Unused finds offered locally at honest prices.
```

Rules:

- Collection IDs are stable.
- Collection labels are public copy.
- Collection order is stable.
- Only `jewelry` is active in M04.
- Do not invent Find counts for Coming Soon Collections.
- Do not add product icons or emoji.
- Do not turn Coming Soon cards into active filters.
- Do not duplicate the current Find data inside the Collection registry.

The Collection registry must load before `app.js`.

---

# 6. APPROVED DISCOVERY CONFIGURATION

Create one authoritative editorial Discovery configuration.

Preferred file:

```text
data/discovery.js
```

Expose:

```text
window.BETWEEN_US_DISCOVERY
```

Required shape:

```text
featuredFindIds
latestFindIds
weeklyFindId
```

Use these exact editorial assignments:

```text
featuredFindIds:
  BU-0001
  BU-0004
  BU-0005

latestFindIds:
  BU-0004
  BU-0005
  BU-0001

weeklyFindId:
  BU-0001
```

Rationale:

- these records have real photographs;
- no dates or history are invented;
- the selection is editorial, not timestamp-derived;
- all IDs already exist and are permanent.

Rules:

- Discovery configuration references only `publicId`.
- No Find fields are duplicated.
- Invalid or missing references must fail validation.
- The order in each array is intentional and must be preserved.
- `Latest Finds` is an editorial view for the current static MVP.
- Documentation must clearly state that it is not yet derived from `createdAt`.
- Future timestamp-based logic is deferred until real timestamps exist.
- Do not modify `createdAt` or `updatedAt`.
- Do not modify the `featured` field during M04 merely to mirror this configuration.

The Discovery configuration must load before `app.js`.

---

# 7. HOME PAGE DISCOVERY STRUCTURE

Refine the M03 home page without replacing the approved shell.

Required order inside `<main>`:

```text
#home
#collections
#featured
#latest
#weekly
#explore
#about
```

The existing header, footer, brand copy, logo, palette, and typography remain approved.

---

# 8. COLLECTIONS EXPERIENCE

## 8.1 Collection preview

The existing Collections section must be rendered from `window.BETWEEN_US_COLLECTIONS`.

Required behavior:

- Jewelry appears as active.
- Other Collections display `Coming Soon`.
- Each card shows label and approved description.
- Jewelry includes a clear action:

```text
Explore Jewelry
```

- That action moves focus or scrolls to `#explore`.
- Coming Soon cards are not clickable.
- No fake links.
- No fake item counts.
- No product icons.
- Preserve the M03 visual restraint.

## 8.2 Explore Collection controls

Add a Collection control area at the top of `#explore`.

Required controls:

- `All Finds`
- `Jewelry`

Current behavior:

- `All Finds` shows all current Finds.
- `Jewelry` shows Finds whose normalized `collection` is `jewelry`.
- Both currently produce five results.
- The system must support additional active Collections automatically when future Find data is added.

Do not show Coming Soon Collections as enabled filter buttons.

Accessibility requirements:

- use real buttons;
- expose selected state with `aria-pressed`;
- keyboard accessible;
- visible focus;
- do not rely only on color;
- update a results summary;
- use a polite `aria-live` region for filter-result changes;
- preserve focus predictably;
- controls must have at least 44px mobile touch targets.

## 8.3 Result summary

Display a concise dynamic summary such as:

```text
5 Finds
```

or:

```text
5 Finds in Jewelry
```

Do not expose internal IDs.

## 8.4 Empty state

Implement a reusable honest empty state for future active Collections with no Finds:

```text
No Finds are available in this Collection yet.
```

This state may be covered by automated tests even if not visible with current data.

---

# 9. FEATURED FINDS

Add section:

```text
Featured Finds
```

Required:

- render the exact configured Finds in configured order;
- use the shared Find-card renderer;
- no duplicated item records;
- preserve image fallback;
- preserve price and availability;
- preserve legacy detail URL format;
- include a short supporting line:

```text
A curated selection from the current catalog.
```

Do not add carousel dependencies.

Use a responsive grid.

---

# 10. LATEST FINDS

Add section:

```text
Latest Finds
```

Required:

- render exact configured Finds in configured order;
- use the shared Find-card renderer;
- include supporting copy:

```text
Recently highlighted Finds from the current collection.
```

Documentation must state:

- this is currently an editorial ordering;
- it is not computed from timestamps;
- no dates were invented;
- real chronological behavior may be introduced later when valid timestamps exist.

Do not show publication dates.

---

# 11. FIND OF THE WEEK

Add section:

```text
Find of the Week
```

Required:

- resolve `weeklyFindId`;
- render one visually distinct editorial feature;
- use the same source Find;
- show:
  - image/fallback;
  - title;
  - public ID;
  - description;
  - price;
  - availability;
  - `View Find` action;
- preserve `item.html?id=N`;
- no countdown;
- no fake discount;
- no urgency language;
- no invented story.

Supporting copy:

```text
One Find selected for a closer look this week.
```

The feature must remain usable on mobile and must not dominate the page excessively.

---

# 12. SHARED FIND-CARD RENDERING

Refactor `app.js` only as much as required to avoid multiple inconsistent card implementations.

Create one reusable rendering path for standard Find cards used by:

- Explore;
- Featured Finds;
- Latest Finds.

Requirements:

- use the normalized Finds as the display source;
- preserve all existing visible values;
- preserve legacy numeric detail links;
- preserve missing-image fallback;
- preserve availability labels;
- preserve original catalog ordering in Explore;
- avoid manually duplicated HTML templates where practical;
- avoid framework/module migration;
- avoid a broad unrelated rewrite.

`window.JEWELRY_ITEMS` must remain available for compatibility and M01/M02 tests.

Do not retire the legacy adapter.

---

# 13. URL AND STATE RULES

M04 must not add new public routes.

Collection selection is page-local state.

Approved:

```text
index.html#explore
```

Not approved:

```text
/collections/jewelry
?collection=jewelry
/find/...
```

Requirements:

- current detail links remain `item.html?id=N`;
- QR payloads remain unchanged;
- share behavior remains unchanged;
- refresh may return Explore to `All Finds`;
- no local storage requirement;
- no history API requirement;
- no deep-link requirement.

---

# 14. RESPONSIVE AND VISUAL REQUIREMENTS

## Desktop

- Discovery sections remain visually distinct but cohesive.
- Grids do not become overly dense.
- Collection filters remain easy to scan.
- Weekly feature balances image and content.
- Existing maximum content width remains coherent.

## Mobile

- No horizontal scrolling.
- Filter buttons may wrap.
- Minimum 44px touch targets.
- Weekly feature stacks cleanly.
- Cards remain readable.
- Headings do not crowd the screen.
- Header remains usable.
- Anchor scroll positions remain readable.

## Motion

- Avoid unnecessary motion.
- Any introduced transition must respect `prefers-reduced-motion`.

---

# 15. ACCESSIBILITY REQUIREMENTS

Required:

- semantic sections with headings;
- logical heading hierarchy;
- accessible Collection filters;
- `aria-pressed` state;
- polite live result summary;
- visible focus;
- disabled/Coming Soon state communicated in text;
- no color-only state;
- shared cards retain meaningful links;
- weekly feature has one clear action;
- SVG and image accessibility from M03 remain intact.

---

# 16. IN SCOPE

Expected additions:

```text
data/collections.js
data/discovery.js
docs/COLLECTIONS_AND_DISCOVERY.md
docs/REPORTS/M04_REPORT.md
tests/discovery/collections.test.mjs
tests/discovery/discovery-config.test.mjs
tests/discovery/discovery-ui.test.mjs
```

Expected modifications may include:

```text
index.html
app.js
styles.css
scripts/validate-baseline.mjs
scripts/lib/baseline-contracts.mjs
docs/VALIDATION.md
docs/PUBLIC_SHELL.md
README.md
```

Optional screenshots:

```text
docs/REPORTS/M04_HOME_DESKTOP.png
docs/REPORTS/M04_HOME_MOBILE.png
docs/REPORTS/M04_FILTER_DESKTOP.png
docs/REPORTS/M04_WEEKLY_MOBILE.png
```

Use the smallest coherent implementation.

---

# 17. PROTECTED FILES

Do not modify:

```text
data/items.js
item.html
item.js
tests/fixtures/legacy-items.snapshot.json
docs/IDENTIFIER_REGISTRY.md
assets/images/*
assets/brand/between-us-mark.svg
.github/workflows/baseline-validation.yml
```

Do not change:

- Find values;
- public IDs;
- legacy IDs;
- slugs;
- titles;
- prices;
- descriptions;
- availability;
- relationships;
- image paths;
- current QR logic;
- current share logic;
- current item-detail routes.

If a protected file appears necessary, stop and request MASTER approval.

---

# 18. OUT OF SCOPE

Do not implement:

- Reserve by Message;
- WhatsApp;
- contact information;
- search;
- sorting controls;
- price filters;
- condition filters;
- collection detail routes;
- slug routes;
- public-ID routes;
- URL query filter state;
- browser history management;
- user accounts;
- seller tools;
- admin dashboard;
- backend;
- database;
- API;
- payments;
- shipping;
- analytics;
- external fonts;
- UI framework;
- CSS framework;
- package manager;
- third-party dependency;
- carousel library;
- new inventory;
- new product photographs;
- invented dates;
- invented conditions;
- invented product stories;
- repository rename;
- GitHub repository rename;
- deployment redesign;
- QR-library replacement.

---

# 19. AUTOMATED CONTRACTS

Add dependency-free tests covering at minimum:

## 19.1 Collection registry

- global exists;
- exactly six records;
- IDs, labels, descriptions, statuses, and order match specification;
- only Jewelry is active;
- IDs unique;
- statuses valid;
- no duplicated Find data;
- no icons or emoji required by the registry.

## 19.2 Discovery configuration

- global exists;
- exact configured IDs and order;
- all references resolve;
- weekly reference resolves;
- no duplicated Find records;
- no invented dates;
- no mutation of normalized Finds.

## 19.3 Script loading

- `data/items.js`, `data/collections.js`, and `data/discovery.js` load before `app.js`;
- existing script order and static architecture remain valid.

## 19.4 Home sections

- required section IDs exist;
- required headings and supporting copy exist;
- collection cards are data-driven;
- Coming Soon is explicit;
- Featured, Latest, and Weekly containers exist;
- Explore filter controls exist;
- result summary/live region exists;
- About and existing shell remain.

## 19.5 Filtering

Using a dependency-free DOM or rendering contract appropriate to the repository:

- All Finds returns five;
- Jewelry returns five;
- unknown/inactive collection cannot become an active filter;
- results preserve order;
- selected state is represented;
- empty state exists;
- legacy detail URLs remain numeric.

## 19.6 Shared rendering

- Featured order is exact;
- Latest order is exact;
- Weekly Find is exact;
- shared cards preserve title, price, availability, image behavior, and `item.html?id=N`;
- no item records are duplicated in discovery files.

## 19.7 Compatibility

All M01, M02, and M03 tests must pass.

Do not weaken prior data, route, brand, QR, share, image, or layout contracts.

---

# 20. DOCUMENTATION

Create:

## `docs/COLLECTIONS_AND_DISCOVERY.md`

Document:

- Collection registry;
- Collection statuses;
- active versus Coming Soon behavior;
- filter behavior;
- results summary;
- editorial discovery configuration;
- Featured ordering;
- Latest editorial limitation;
- Find of the Week;
- source-of-truth boundaries;
- how to activate a future Collection safely;
- how to add future Find references;
- deferred features.

Update:

## `docs/PUBLIC_SHELL.md`

Add the M04 section order and behavior.

## `docs/VALIDATION.md`

Add M04 validation coverage and commands.

## `README.md`

Add a concise description of Collections and discovery views without overstating unavailable inventory.

Create:

## `docs/REPORTS/M04_REPORT.md`

Use Section 24.

---

# 21. VALIDATION COMMANDS

Run at minimum:

```bash
node --check app.js
node --check item.js
node --check data/items.js
node --check data/collections.js
node --check data/discovery.js
node --test tests/baseline/*.test.mjs
node --test tests/domain/*.test.mjs
node --test tests/brand/*.test.mjs
node --test tests/discovery/*.test.mjs
node scripts/validate-baseline.mjs
git diff --check
git diff --cached --check
git status --short
```

Also confirm the existing GitHub Actions workflow still runs the same validator.

---

# 22. VISUAL VALIDATION

Start a local static server and inspect:

```text
/
index.html#collections
index.html#featured
index.html#latest
index.html#weekly
index.html#explore
item.html?id=1
item.html?id=2
```

Review at desktop and mobile widths.

Required checks:

- section order;
- Collection cards;
- Coming Soon honesty;
- All Finds filter;
- Jewelry filter;
- selected-state clarity;
- live result summary;
- Featured order;
- Latest order;
- Weekly Find;
- mobile wrapping;
- no horizontal overflow;
- card consistency;
- missing-image fallback;
- availability states;
- navigation anchors;
- no new console errors;
- no broken assets;
- all five numeric detail URLs.

If supported, create the optional screenshots listed in Section 16.

M04 remains pending owner visual acceptance.

---

# 23. ACCEPTANCE CRITERIA

M04 implementation candidate is complete only when:

1. `window.BETWEEN_US_COLLECTIONS` exists.
2. It contains the exact six approved Collections in order.
3. Jewelry is active.
4. All other Collections are honestly Coming Soon.
5. Collection preview is rendered from the registry.
6. `window.BETWEEN_US_DISCOVERY` exists.
7. Featured IDs and order are exact.
8. Latest IDs and order are exact.
9. Weekly ID is exact.
10. No dates or Find history are invented.
11. All Finds filter works.
12. Jewelry filter works.
13. Filter controls are accessible.
14. Results summary/live region works.
15. Empty-state behavior exists.
16. Featured Finds render correctly.
17. Latest Finds render correctly.
18. Find of the Week renders correctly.
19. Shared cards preserve current values and URLs.
20. Original Explore order remains unchanged.
21. No protected files change.
22. Existing item detail behavior remains unchanged.
23. Existing QR/share behavior remains unchanged.
24. Existing data, identifiers, relationships, and images remain unchanged.
25. No new route or dependency is added.
26. All baseline, domain, brand, and discovery tests pass.
27. Desktop and mobile visual validation pass.
28. Documentation is complete.
29. One clear milestone commit exists.
30. Working tree is clean.
31. Nothing is pushed or merged.
32. Final acceptance state is:

```text
Implementation candidate — pending owner visual acceptance
```

---

# 24. REQUIRED FINAL REPORT FORMAT

Return a concise report using exactly these sections:

## M04 Result

- Status: PASS, PARTIAL, or BLOCKED
- Acceptance state: `Implementation candidate — pending owner visual acceptance`
- Branch
- Commit
- Base commit
- Rollback reference

## Repository inspected

- Important files and M01–M03 safeguards confirmed

## Collection system implemented

- Registry global
- Collection records
- Active Collection
- Coming Soon Collections
- Filter behavior
- Empty state

## Discovery system implemented

- Discovery global
- Featured Finds
- Latest Finds
- Find of the Week
- Editorial-order limitation

## Files created

- Exact paths

## Files modified

- Exact paths
- State explicitly whether any protected file changed

## Files removed

- Exact paths or `None`

## Validation executed

- Exact commands
- PASS/FAIL
- Total tests

## Visual validation

- Desktop home
- Mobile home
- Collections
- Filters
- Featured
- Latest
- Weekly
- Detail routes
- Missing image
- Availability states
- Console
- Screenshots created or not created

## Compatibility results

- Data parity
- Numeric routes
- Public IDs/slugs
- Related Finds
- QR/share
- Image fallback
- M03 brand shell
- Responsive behavior

## Warnings and known limitations

- Only Jewelry currently active
- Coming Soon Collections
- Latest is editorial, not timestamp-derived
- Deferred search and advanced filtering
- Deferred reservation messaging
- External QR dependency
- Any remaining visual concerns

## Git status

- Final branch
- Final commit hash
- Working-tree state
- Push status

## Owner visual-review instructions

Provide:

- exact local preview command;
- exact home URL;
- exact anchors to inspect;
- exact real-image detail URL;
- exact missing-image detail URL;
- what the owner should inspect.

## Recommended next step

The expected recommendation is:

```text
Owner visual review and acceptance of M04.
```

Do not begin M05.

---

# 25. GIT REQUIREMENTS

After all validation passes:

```bash
git add .
git commit -m "feat: add Collections and discovery"
git status --short
```

Requirements:

- do not push;
- do not merge;
- do not create a remote tag;
- leave the working tree clean;
- report the final commit;
- report the base commit as rollback reference.

---

# 26. STOP CONDITIONS

Stop and report without destructive changes if:

- the repository is not clean;
- `main` differs from `origin/main`;
- commit `34981ca` is not present;
- any M01–M03 validation fails;
- the M04 package is missing;
- a protected file appears necessary;
- current item-detail behavior would change;
- numeric routes, QR, or share behavior cannot be preserved;
- new dependencies appear necessary;
- Collection status or discovery references are ambiguous;
- required editorial IDs do not resolve;
- visual validation reveals a blocking regression.

Wait for MASTER review after submitting the M04 report.
