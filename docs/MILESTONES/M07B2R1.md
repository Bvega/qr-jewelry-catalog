# BETWEEN US PLATFORM — REPAIR IMPLEMENTATION PACKAGE

## M07B-2R1 — Seller Invitation and Password Setup

**Version:** 1.0  
**Status:** Approved for implementation  
**Repository:** `/Users/miniboli/DEV/qr-jewelry-catalog`  
**Target branch:** `fix/m07b2r1-seller-account-activation`  
**Required base:** `8e202f1d1e9792d808efa2b592476dbcfc01d702` or a later clean `main` containing it  
**Environment:** Codex  
**Model:** GPT-5.6 Sol — Extra High

---

# 1. PURPOSE

Repair the missing first-time seller-account activation flow discovered during the controlled remote owner smoke test.

M07B-2 currently supports email/password sign-in, session restoration, sign-out, and owner/editor role authorization. It does not provide a page where an invited allowlisted seller can finish account setup by creating a password.

Add a private activation page at:

```text
/admin/activate.html
```

The page must accept the authenticated session created by a valid Supabase invitation link, independently verify that the current user is allowlisted as `owner` or `editor`, let that user set and confirm a password, then sign the user out and direct them to the normal Seller Catalog Manager sign-in page.

This repair must preserve the existing Auth user UUID and the existing `private.catalog_admins` owner allowlist row.

# 2. DISCOVERED BLOCKER

The remote owner Auth user exists but remains unconfirmed because the invitation was never completed.

The Supabase project currently uses:

```text
Site URL: http://localhost:3000
Redirect URLs: none
```

On this development machine, `localhost:3000` opens another local application, Open WebUI. The Seller Catalog Manager is served at:

```text
http://127.0.0.1:3000/admin/
```

The previous invitation therefore opened the wrong application.

Do not solve this by deleting or recreating the owner user. Do not change the allowlisted UUID.

# 3. ROLE

You are the implementation engine.

Inspect first. Implement second. Validate third. Commit locally. Report last.

Follow this package exactly. Do not redesign the Seller Catalog Manager. Do not change scope. Do not invent owner data. Do not use or print real credentials. Do not push, merge, tag, or modify remote Supabase settings.

# 4. PRECONDITIONS

Before modifying files, run:

```bash
cd /Users/miniboli/DEV/qr-jewelry-catalog
git status --short
git branch --show-current
git fetch origin
git rev-parse HEAD
git rev-parse main
git rev-parse origin/main
git merge-base --is-ancestor 8e202f1d1e9792d808efa2b592476dbcfc01d702 main
git check-ignore -v .env.local
git check-ignore -v admin/config.js
node scripts/validate-baseline.mjs
npm run admin:validate
node scripts/validate-supabase-foundation.mjs
node --version
npm --version
```

Confirm:

- current branch is `main`;
- working tree is clean;
- `main` equals `origin/main`;
- the required base is an ancestor of `main`;
- `.env.local` and `admin/config.js` are ignored;
- inherited validation passes;
- no secret value is printed.

Then create:

```bash
git switch -c fix/m07b2r1-seller-account-activation
```

Codex must create the branch. The user must not be instructed to create it manually.

# 5. NON-NEGOTIABLE BOUNDARIES

Do not:

- delete or recreate the remote owner Auth user;
- change the owner UUID or allowlist row;
- make remote Auth, URL Configuration, email-template, or database changes;
- add a public signup flow;
- add an anonymous password-creation flow;
- add a server-side secret key;
- add a service-role key;
- embed `.env.local` values into committed bundles;
- log passwords, URL fragments, access tokens, refresh tokens, sessions, or user objects;
- parse or display token values;
- expose admin membership before an authenticated session exists;
- weaken `current_catalog_admin_role()`;
- change public catalog behavior or its data source;
- migrate the four intake Finds;
- deploy Cloudflare;
- modify accepted intake CSVs or product photos;
- push, merge, or tag.

# 6. ACCEPTED TECHNICAL FOUNDATION

Preserve the framework-free M07B-2 architecture:

- semantic HTML;
- modular vanilla JavaScript;
- manager-specific CSS;
- `@supabase/supabase-js`;
- deterministic `esbuild` bundles;
- Node built-in tests;
- ignored `admin/config.js`;
- loopback-only static server.

No new framework or general application server is allowed. No new dependency should be required. If a dependency is unavoidable, stop and report the reason before adding it.

# 7. REQUIRED FILES

Create or update an equivalent clean structure:

```text
admin/
  activate.html
  assets/
    activate.js
    app.js
    styles.css

admin-src/
  activation.js
  auth.js
  password.js
  styles.css
  ui.js

scripts/
  build-admin.mjs
  validate-seller-manager.mjs

tests/
  seller-manager/
    activation.test.mjs
    auth.test.mjs
    password.test.mjs
    shell.test.mjs

docs/
  SELLER_MANAGER_LOCAL_SETUP.md
  SELLER_ACCOUNT_ACTIVATION.md
  REPORTS/
    M07B2R1_REPORT.md
```

Do not commit `admin/config.js`.

# 8. ACTIVATION PAGE SHELL

Create `admin/activate.html` with:

- Between Us private-workspace branding using local assets;
- page title identifying seller account setup;
- accessible loading/status region;
- invalid or expired invitation state;
- access-denied state;
- password form;
- new-password field;
- confirm-password field;
- submit button;
- success state with a link to `/admin/`;
- no email field;
- no signup control;
- no public password-reset request form;
- no owner identifier;
- no inline JavaScript;
- no `unsafe-eval`.

Use the same strict local-asset CSP pattern as `/admin/`, allowing only the existing Supabase HTTPS/WSS connections and required image sources.

Load:

```text
./config.js
./assets/activate.js
```

with `defer`.

# 9. INVITATION SESSION HANDLING

Use the Supabase browser client’s normal URL-session detection.

Do not manually decode, print, store, or render access tokens or refresh tokens.

Required flow:

1. create the browser client from the existing generated safe config;
2. subscribe immediately to Auth state changes;
3. allow the SDK to process the invitation redirect;
4. obtain the resulting authenticated session through the SDK;
5. handle initial-session and signed-in timing without duplicate processing;
6. reject a missing session with a neutral invalid/expired invitation message;
7. call `public.current_catalog_admin_role()` only after authentication;
8. allow only `owner` and `editor`;
9. sign out and deny any authenticated non-admin;
10. show the password form only after role authorization succeeds.

The page must never unlock the normal catalog manager merely because the invitation session exists.

# 10. PASSWORD SETUP

Create a small testable password module.

Client-side rules:

- password is required;
- confirmation is required;
- values must match;
- minimum length: 12 characters;
- maximum length: 128 characters;
- do not trim or silently alter the password;
- do not require or display the current password;
- do not log either field.

On valid submission, call:

```text
supabase.auth.updateUser({ password })
```

Required behavior:

- prevent duplicate submission;
- disable the submit control while pending;
- return neutral, recoverable errors;
- report success only after Supabase confirms the update;
- clear both password fields after completion or failure;
- scrub invitation query/hash material from the address bar using `history.replaceState` after the SDK has established the session;
- sign out after successful password creation;
- show a success message;
- direct the seller to `/admin/` for a fresh email/password sign-in.

Do not automatically open the catalog manager under the invitation session.

# 11. ERROR AND SECURITY STATES

Provide neutral UI states for:

- configuration missing or invalid;
- invitation missing;
- invitation invalid or expired;
- Auth initialization failure;
- role-probe failure;
- authenticated but not allowlisted;
- password validation failure;
- password update rejection;
- sign-out failure after password update;
- successful account setup.

Do not expose arbitrary-email registration status, another user’s role, Auth error internals, tokens, UUIDs, session JSON, or stack traces.

# 12. EXISTING SIGN-IN FLOW

Preserve the accepted `/admin/` sign-in and role gate.

Minimal integration changes are allowed only where required to share safe Auth helpers or avoid duplicated logic.

Do not add signup. Do not add “forgot password” in this repair. Do not alter catalog CRUD, image workflow, RLS policies, Storage policies, or the role-probe migration.

No new database migration is expected.

# 13. BUILD

Update the deterministic build so it produces both:

```text
admin/assets/app.js
admin/assets/activate.js
```

and the existing stylesheet output.

Requirements:

- browser-compatible bundles;
- exact pinned dependencies remain;
- no `.env.local` values embedded;
- no source maps containing secrets;
- unresolved imports fail the build;
- public runtime files remain unchanged.

# 14. TESTING

Add focused Node tests proving at least:

## Activation shell

- `admin/activate.html` exists;
- local branding and accessible states exist;
- new-password and confirm-password fields exist;
- no email field exists;
- no signup exists;
- no inline script exists;
- strict CSP exists;
- only local config and bundle scripts load.

## Session and authorization

- missing session fails closed;
- valid invite session does not bypass role authorization;
- owner and editor may reach password setup;
- non-admin is signed out and denied;
- role-probe error fails closed;
- duplicate Auth events do not submit or initialize twice;
- token/session values are not logged or rendered.

## Password setup

- minimum and maximum boundaries;
- mismatched passwords rejected;
- password is not trimmed;
- duplicate submit prevented;
- `updateUser({ password })` is called only after authorization;
- successful update signs out;
- URL query/hash is scrubbed only after session establishment;
- success directs to `/admin/`;
- errors clear sensitive fields and remain recoverable.

## Regression

- existing M07B-2 Seller Manager tests remain green;
- public runtime and intake protected files remain unchanged;
- no new migration appears;
- `admin/config.js` remains ignored and uncommitted.

# 15. VALIDATION

Run:

```bash
npm ci
npm run admin:build
npm run admin:validate
node scripts/validate-content-intake.mjs
node scripts/summarize-content-intake.mjs
node scripts/validate-supabase-foundation.mjs
node scripts/validate-baseline.mjs
git diff --check
git status --short
```

Docker and local Supabase are not required unless implementation unexpectedly changes database-facing behavior.

If any database migration, policy, or SQL file changes, stop and report scope violation before committing.

# 16. LOCAL MANUAL SMOKE

Use fictional/local behavior only during implementation.

Run:

```bash
npm run admin:config
npm run admin:serve
```

Verify:

- `/admin/` still loads;
- `/admin/activate.html` loads;
- activation page contains no signup;
- activation page does not expose config values;
- a plain visit without an invite shows a neutral invalid/expired state;
- page source and console contain no secret or token/session log;
- POST remains unavailable on the local server.

Do not use the real owner invitation or write to remote Auth during Codex implementation.

# 17. PROTECTED FILES

The following must remain unchanged:

```text
data/items.js
data/collections.js
data/discovery.js
data/media.js
data/reservation.js
data/permalinks.js
content-intake/finds.csv
content-intake/photo-manifest.csv
content-intake/photos/*
tests/fixtures/legacy-items.snapshot.json
docs/IDENTIFIER_REGISTRY.md
supabase/migrations/*
supabase/tests/database/*
```

Public runtime files may change only if a nonbehavioral shared brand asset is strictly required. Any such change must be separately justified and prove exact behavior parity.

# 18. DOCUMENTATION

Create `docs/SELLER_ACCOUNT_ACTIVATION.md` documenting:

- why activation is separate from sign-in;
- exact local activation URL;
- the role gate;
- password-setup behavior;
- URL/token handling;
- success sign-out and fresh sign-in;
- no signup;
- no secret-key requirement;
- expired-invitation recovery;
- manual Supabase dashboard steps that occur only after MASTER acceptance.

Update `docs/SELLER_MANAGER_LOCAL_SETUP.md` with the activation route and controlled manual smoke procedure.

Create `docs/REPORTS/M07B2R1_REPORT.md`.

Do not include owner email, owner UUID, password, token, key, or invitation URL.

# 19. POST-ACCEPTANCE MANUAL DASHBOARD PLAN

Document but do not execute these steps:

1. keep the existing owner Auth user and allowlist row;
2. set the temporary development Site URL to:

```text
http://127.0.0.1:3000/admin/activate.html
```

3. add exact Redirect URLs:

```text
http://127.0.0.1:3000/admin/activate.html
http://127.0.0.1:3000/admin/
```

4. save the URL Configuration;
5. resend the invitation to the existing unconfirmed owner user;
6. open the new invitation while `npm run admin:serve` is running;
7. set a new password on the activation page;
8. confirm the user becomes verified;
9. sign in through `/admin/`;
10. verify the owner role gate and empty remote catalog;
11. restore or replace Site URL during M07B-4 production deployment.

Do not use `localhost`; use exact `127.0.0.1`.

# 20. COMMIT

After all validation passes:

```bash
git add \
  admin/activate.html \
  admin/assets/activate.js \
  admin/assets/app.js \
  admin/assets/styles.css \
  admin-src \
  scripts/build-admin.mjs \
  scripts/validate-seller-manager.mjs \
  tests/seller-manager \
  docs/SELLER_ACCOUNT_ACTIVATION.md \
  docs/SELLER_MANAGER_LOCAL_SETUP.md \
  docs/REPORTS/M07B2R1_REPORT.md

git commit -m "fix: add seller account activation"
```

Include only files actually changed.

Do not push. Do not merge. Do not tag.

# 21. REQUIRED FINAL REPORT

Return:

```text
# M07B-2R1 Final Report

## Result
- Status:
- Acceptance state:
- Branch:
- Commit:
- Commit message:
- Base/rollback reference:

## Root cause confirmed

## Activation flow delivered

## Security behavior

## Files created

## Files modified

## Files removed

## Validation executed

## Protected-scope verification

## Warnings and known limitations

## Git status

## Recommended next step
```

Required acceptance state:

```text
Implementation candidate — pending MASTER acceptance
```

Recommended next step:

```text
MASTER review and acceptance, followed by controlled remote owner activation smoke. Do not begin M07B-3.
```
