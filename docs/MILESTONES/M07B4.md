# BETWEEN US PLATFORM — IMPLEMENTATION PACKAGE

## M07B-4 — Deployment and Acceptance

**Version:** 1.0
**Status:** Approved for implementation
**Repository:** `~/DEV/qr-jewelry-catalog`
**Target branch:** `feature/m07b4-deployment-acceptance`
**Required base:** `00c5def9efa362cbc5a7da89f53d35bdbb24c76e` or a later clean `main` containing it
**Environment:** Codex Local
**Model:** GPT-5.6 Sol — Extra High
**Execution mode:** Complete milestone in one implementation pass whenever technically possible

---

# 1. PURPOSE

Deploy the accepted Between Us public catalog and Seller Catalog Manager as a
production static site through GitHub Pages and GitHub Actions.

M07B-4 must:

- preserve the accepted public catalog exactly;
- deploy the Seller Catalog Manager as an authenticated private workspace;
- connect the Manager to the existing remote Supabase project through a
  browser-safe publishable key;
- exclude temporary activation and M07B-3 migration tooling from the
  production artifact;
- add deterministic deployment validation and a controlled acceptance runbook;
- provide a documented rollback path.

M07B-4 must not publish the four imported Finds or switch the public catalog
from static repository data to Supabase.

# 2. ACCEPTED FOUNDATION

Complete and accepted:

- M00 through M07A;
- M07B-1 Supabase Foundation;
- remote Supabase bootstrap;
- M07B-2 Seller Catalog Manager;
- M07B-2R1 Seller Invitation and Password Setup;
- controlled owner activation and authorization smoke;
- M07B-3 Controlled Catalog Data Migration;
- controlled remote import of `BU-0006` through `BU-0009`;
- M07B-3 remote import acceptance at commit
  `00c5def9efa362cbc5a7da89f53d35bdbb24c76e`.

Current remote state:

- four imported Finds exist;
- all four are hidden;
- all four are unfeatured;
- all four have verified primary-photo metadata and Storage objects;
- the public catalog still exposes only the five accepted static Finds;
- the next generated public ID is `BU-0010`.

# 3. TWO-STAGE SAFETY MODEL

## Stage A — implementation and local verification

Codex implements the complete milestone locally, validates it, and creates one
local implementation commit.

Codex must not:

- configure GitHub Pages remotely;
- create or change GitHub Actions variables or secrets;
- trigger or approve a remote deployment;
- use real owner credentials;
- sign into remote Supabase;
- write any remote database or Storage data;
- publish any Find;
- push, merge, tag, or release.

## Stage B — controlled deployment and acceptance

Only after MASTER accepts Stage A:

- configure the required browser-safe GitHub repository variables;
- enable GitHub Pages with GitHub Actions as the publishing source;
- merge and push the accepted implementation;
- observe the deployment workflow;
- perform the production acceptance runbook;
- record final acceptance.

# 4. PREFLIGHT

Before modifying files, Codex must inspect the repository and run:

```bash
git status --short
git branch --show-current
git fetch origin
git rev-parse HEAD
git rev-parse main
git rev-parse origin/main
git merge-base --is-ancestor 00c5def9efa362cbc5a7da89f53d35bdbb24c76e main
git check-ignore -v .env.local
git check-ignore -v admin/config.js
npm ci
npm run validate:baseline
npm run admin:validate
npm run migration:validate
npm run migration:test
```

Confirm:

- branch is `main`;
- working tree is clean;
- local `main` equals `origin/main`;
- the accepted M07B-3 commit is an ancestor;
- ignored local configuration remains ignored;
- inherited validation passes;
- no real deployment configuration is printed.

Then Codex creates:

```bash
git switch -c feature/m07b4-deployment-acceptance
```

Codex creates the branch. The user does not create it manually.

# 5. DEPLOYMENT ARCHITECTURE

Use a custom GitHub Pages workflow with separate build and deploy jobs.

Required workflow:

```text
.github/workflows/deploy-pages.yml
```

Required behavior:

- triggers on:
  - pull requests targeting `main`, for build and validation only;
  - pushes to `main`, for build, validation, and deployment;
  - manual `workflow_dispatch`, for controlled redeployment;
- uses GitHub-hosted Ubuntu;
- checks out the exact triggering revision;
- installs the repository-locked Node dependencies with `npm ci`;
- runs inherited validation;
- builds the Seller Manager production assets;
- creates a strict allowlisted Pages artifact;
- validates the final artifact before upload;
- uploads the artifact through the official GitHub Pages artifact action;
- deploys only from `main`, never from a pull request;
- uses the `github-pages` environment;
- grants only:
  - `contents: read`;
  - `pages: write`;
  - `id-token: write`;
- uses deployment concurrency so only one Pages deployment runs at a time;
- exposes the deployed URL as the deploy-job environment URL.

Use the accepted official action majors current for this package:

```text
actions/checkout@v6
actions/setup-node@v6
actions/configure-pages@v5
actions/upload-pages-artifact@v4
actions/deploy-pages@v4
```

Do not upload the repository root as the Pages artifact.

# 6. STRICT PRODUCTION ARTIFACT

Create:

```text
deployment/pages-manifest.json
scripts/build-pages-artifact.mjs
scripts/validate-pages-artifact.mjs
tests/deployment/pages-artifact.test.mjs
```

Build output:

```text
dist/pages/
```

The tracked manifest is the production allowlist. Codex must inspect the
current public HTML, JavaScript, CSS, data, images, QR assets, and Manager
dependencies and list only files required at runtime.

The builder must:

- start from an empty `dist/pages/`;
- copy only allowlisted runtime files;
- preserve bytes for accepted public runtime files;
- build current Manager assets before copying them;
- generate a deployment-only `admin/runtime-config.js`;
- add `.nojekyll`;
- refuse missing, duplicate, outside-root, dot-path, or symlink sources;
- fail if an unexpected output file appears;
- use deterministic file ordering;
- never modify tracked source assets while assembling the artifact.

The artifact may contain:

- accepted public catalog pages;
- accepted public runtime scripts and data;
- accepted brand, product, QR, and fallback assets;
- `admin/index.html`;
- the production Manager bundle and stylesheet;
- generated `admin/runtime-config.js`;
- `.nojekyll`;
- an existing public `404.html` only when it is already part of the accepted
  runtime or is added without redesigning the application.

# 7. FORBIDDEN PRODUCTION CONTENT

The production artifact must not contain:

```text
.git/
.github/
.env
.env.*
admin/config.js
admin/activate.html
admin/migrate-intake.html
admin/assets/activate.js
admin/assets/migrate-intake.js
admin-src/
content-intake/
deployment/
docs/
migration/
node_modules/
scripts/
supabase/
tests/
package.json
package-lock.json
chatGPT_Todo.txt
README.md
```

It must also exclude:

- source maps;
- owner notes;
- credentials;
- sessions;
- access tokens;
- database passwords;
- secret or service-role keys;
- owner email or UUID;
- the M07B-3 plan and maintenance source files;
- temporary activation and migration routes;
- any public runtime reference to `BU-0006` through `BU-0009`.

The activation and migration source files remain in the repository for local,
controlled maintenance history. They are not deployed.

# 8. DEPLOYMENT RUNTIME CONFIGURATION

The production Manager requires exactly these browser-safe values:

```text
BETWEEN_US_SUPABASE_URL
BETWEEN_US_SUPABASE_PUBLISHABLE_KEY
BETWEEN_US_SUPABASE_PROJECT_REF
```

Use GitHub Actions repository variables through the `vars` context. These
values are public deployment configuration because the resulting static
browser file can be inspected by any visitor.

The workflow maps them only for the artifact-build step:

```text
SUPABASE_URL
SUPABASE_PUBLISHABLE_KEY
SUPABASE_PROJECT_REF
```

Reuse the existing configuration validation and serialization logic from
`scripts/generate-admin-config.mjs`; refactor safely when necessary rather
than duplicating weaker validation.

The deployment generator must:

- accept a modern `sb_publishable_...` key;
- retain compatibility with a validated legacy `anon` key;
- reject blank or malformed values;
- reject mismatched project URL and project reference;
- reject local loopback configuration in production mode;
- reject `sb_secret_...`, service-role, database, access-token, and other
  privileged values;
- write only `dist/pages/admin/runtime-config.js`;
- never write or commit the production value to `admin/config.js`;
- never print configuration values to logs.

No secret key is required for this deployment.

# 9. PUBLIC CATALOG FREEZE

The deployed public catalog must remain on the current static source.

Preserve:

- the five original public Finds and their order;
- permanent `find.html?id=BU-NNNN` URLs;
- registered slug aliases;
- legacy numeric `item.html?id=N` routes;
- existing QR destinations;
- current Collections, Featured, Latest, and Find of the Week behavior;
- Reserve by Message;
- Share Find and Copy Link;
- image fallback behavior;
- project-subpath compatibility.

Do not expose any of:

```text
BU-0006
BU-0007
BU-0008
BU-0009
```

Do not add a Supabase public read path in this milestone.

# 10. SELLER MANAGER PRODUCTION CONTRACT

Production route:

```text
/qr-jewelry-catalog/admin/
```

The exact base path must be derived from GitHub Pages metadata or preserved
through relative links. Do not hardcode a domain.

Preserve:

- allowlisted email/password sign-in;
- role verification;
- exact `owner` and accepted seller-role behavior;
- no public signup;
- no password-reset request;
- authenticated remote catalog loading;
- create, edit, image replace, draft, publish, hide, archive, and restore
  behavior already accepted by M07B-2;
- sign out;
- noindex/nofollow metadata;
- current restrictive CSP unless a narrowly justified production adjustment
  is required and fully tested.

The route is publicly reachable static HTML. Privacy and authorization come
from Supabase Auth, RLS, Storage policies, and the existing role probe. Never
treat an unlinked URL as access control.

# 11. VALIDATION AND TESTS

Add package scripts with clear names, including:

```text
pages:build
pages:validate
pages:test
pages:check
pages:serve
```

`pages:check` must perform the complete local deployment check in one command.

Use fictional valid browser configuration for local tests. Never require real
production values for Codex validation.

Tests must prove:

## Artifact integrity

- artifact starts clean;
- manifest is deterministic;
- every manifest source exists;
- every runtime reference resolves inside the artifact;
- no unexpected file exists;
- no symlink exists;
- no forbidden directory or filename exists;
- no source map exists;
- `.nojekyll` exists;
- repeated builds with unchanged sources produce identical file bytes and
  manifest-derived inventory.

## Configuration safety

- modern publishable key accepted;
- validated legacy anon key accepted;
- secret and service-role keys rejected;
- blank values rejected;
- mismatched URL/reference rejected;
- loopback URL rejected in production mode;
- output contains only browser-safe fields;
- configuration values are not logged;
- output exists only inside `dist/pages/admin/runtime-config.js`.

## Public compatibility

- five accepted public Finds remain available;
- `BU-0006` through `BU-0009` are absent from public data and rendered pages;
- all permanent ID routes remain valid;
- all legacy numeric routes remain valid;
- all registered slug aliases remain valid;
- QR destinations remain unchanged;
- relative navigation works under the repository project subpath;
- accepted public source files copied to the artifact remain byte-identical.

## Manager deployment

- `admin/index.html`, Manager bundle, stylesheet, and runtime config exist;
- activation and migration routes and bundles are absent;
- Manager HTML references only deployed assets;
- no signup link or public registration flow appears;
- CSP permits only the required Supabase network targets;
- no privileged key name or credential appears in the artifact.

## Workflow

- pull requests cannot deploy;
- only `main` push or controlled manual dispatch can deploy;
- deploy job needs successful build;
- permissions are least privilege;
- environment is `github-pages`;
- artifact path is exactly `dist/pages`;
- workflow never uploads the repository root;
- real browser configuration is read from GitHub variables only during build.

Run at minimum:

```bash
npm run validate:baseline
npm run admin:validate
npm run migration:validate
npm run migration:test
npm run pages:check
git diff --check
```

Docker and remote Supabase are not required for Stage A unless Codex changes a
database or policy file. No database or policy file should be changed in this
milestone.

# 12. LOCAL PRODUCTION PREVIEW

`npm run pages:serve` must serve only `dist/pages/` on loopback and print the
exact local public and Manager preview URLs.

It must:

- never serve repository internals;
- never expose M07B-3 maintenance resources;
- bind only to `127.0.0.1`;
- use a deterministic default port with an optional validated override;
- support clean shutdown with `Ctrl+C`.

Codex performs a local smoke test of:

- public home;
- one permanent-ID Find detail;
- one legacy numeric route;
- one registered slug alias;
- `/admin/`;
- absence of activation and migration routes;
- absence of repository-internal paths.

Do not use real owner credentials during Stage A.

# 13. DEPLOYMENT AND ACCEPTANCE RUNBOOK

Create:

```text
docs/M07B4_DEPLOYMENT_ACCEPTANCE_RUNBOOK.md
```

The runbook must be exact, one-step-at-a-time, and safe for the owner.

It must cover:

1. accepted implementation commit and clean `main`;
2. required GitHub repository variable names;
3. how to copy the browser-safe values from the existing local configuration
   without placing them in chat, commit history, screenshots, or shell history;
4. enabling GitHub Pages with GitHub Actions as publishing source;
5. pushing the accepted merge;
6. monitoring the build and deploy jobs;
7. recording the production URL;
8. public-site acceptance;
9. QR and legacy-link compatibility;
10. production owner sign-in;
11. exact remote Manager count of 4 of 4 imported Finds;
12. confirmation that all four imported Finds remain hidden and unfeatured;
13. sign out;
14. confirmation that activation and migration routes return unavailable;
15. confirmation that repository internals are unavailable;
16. rollback;
17. final acceptance record.

The runbook must never ask the owner to paste a key, password, token, UUID, or
email into ChatGPT.

# 14. ROLLBACK

Provide two rollback levels.

## Deployment-content rollback

- identify the last successful Pages workflow run;
- redeploy the last accepted revision through a controlled workflow rerun or
  an accepted revert commit;
- verify public and Manager routes after rollback.

## Emergency shutdown

- disable the Pages publishing source through repository settings when the
  deployed static site must be removed immediately;
- do not modify remote Supabase data as part of a Pages shutdown.

The runbook must explain that disabling Pages does not delete the remote
Supabase catalog.

# 15. DOCUMENTATION UPDATES

Update only documentation affected by the implementation:

```text
README.md
docs/VALIDATION.md
docs/SUPABASE_LOCAL_DEVELOPMENT.md
chatGPT_Todo.txt
```

Preserve historical acceptance records.

Documentation must state clearly:

- public catalog remains static;
- Seller Manager uses remote Supabase only after authenticated sign-in;
- publishable browser configuration is public, not a privileged secret;
- service-role and secret keys are forbidden;
- temporary activation and migration pages are not deployed;
- M07B-4 deployment does not publish `BU-0006` through `BU-0009`.

# 16. EXPECTED IMPLEMENTATION FILES

Expected additions or modifications may include:

```text
.github/workflows/deploy-pages.yml
deployment/pages-manifest.json
scripts/build-pages-artifact.mjs
scripts/validate-pages-artifact.mjs
scripts/serve-pages-artifact.mjs
scripts/generate-pages-runtime-config.mjs
tests/deployment/pages-artifact.test.mjs
package.json
package-lock.json
README.md
docs/VALIDATION.md
docs/SUPABASE_LOCAL_DEVELOPMENT.md
docs/M07B4_DEPLOYMENT_ACCEPTANCE_RUNBOOK.md
chatGPT_Todo.txt
```

Codex may choose a smaller or slightly different internal file breakdown after
inspecting the repository, but it must preserve every contract in this
package.

Do not modify:

- accepted public catalog records;
- public identifier mappings;
- content-intake CSV values;
- source intake photos;
- M07B-3 migration plan;
- Supabase migrations;
- RLS or Storage policies;
- remote data.

# 17. REQUIRED CODEX REPORT

Return one concise technical report containing:

```text
M07B-4 IMPLEMENTATION REPORT

Branch:
Base commit:
Implementation commit:

Files created:
Files modified:

Deployment architecture:
Artifact allowlist:
Production exclusions:
Runtime configuration safety:
Public compatibility:
Manager deployment:
Workflow behavior:
Rollback design:

Validation commands:
Validation results:

Remote writes performed:
Push performed:
Merge performed:
Deployment performed:

Risks or follow-up:
Final status:
```

Required final statements:

```text
Remote writes performed: No
Push performed: No
Merge performed: No
Deployment performed: No
```

# 18. ACCEPTANCE CRITERIA

Stage A is acceptable only when:

- implementation branch is based on accepted clean `main`;
- all inherited validation passes;
- one-command `pages:check` passes;
- strict artifact allowlist is implemented;
- production artifact contains no repository internals;
- production config accepts only browser-safe keys;
- public catalog remains unchanged;
- `BU-0006` through `BU-0009` remain absent publicly;
- Manager production assets are complete;
- activation and migration routes are excluded;
- workflow cannot deploy pull requests;
- no remote action occurred;
- repository is clean after the local implementation commit;
- Codex returns the complete required report.

Stage B is acceptable only when:

- GitHub Pages deploys from the accepted `main`;
- production public site passes all compatibility checks;
- production Manager authenticates the existing owner;
- Manager displays exactly 4 of 4 imported Finds;
- all four remain hidden and unfeatured;
- temporary maintenance routes are unavailable;
- owner signs out;
- rollback instructions are verified;
- final acceptance is recorded without private data.

# 19. OUT OF SCOPE

Do not implement:

- public Supabase catalog reads;
- automatic publication of hidden Finds;
- search redesign;
- payment;
- shipping;
- customer accounts;
- public signup;
- password reset;
- new seller onboarding;
- custom domain;
- analytics;
- service worker or offline mode;
- backend server deployment;
- Edge Functions;
- email delivery;
- new database migrations;
- new catalog content.

# 20. CODEX EXECUTION INSTRUCTION

Execute this milestone as one complete implementation package whenever
technically possible.

Inspect first, implement the whole accepted scope, run all validation, repair
milestone-related failures, create one local implementation commit, and return
the required report.

Do not stop after scaffolding. Do not ask the user to create the branch. Do not
perform remote deployment, push, merge, or tag.
