# BETWEEN US PLATFORM — IMPLEMENTATION PACKAGE

## Milestone M03 — Between Us Brand and Public Shell

**Version:** 1.0  
**Status:** Approved for implementation candidate  
**Target repository:** `/Users/miniboli/DEV/qr-jewelry-catalog`  
**Target branch:** `migration/m03-between-us-shell`  
**MASTER source of truth:** `docs/MASTER_SPECIFICATION.md`  
**Required baseline:** M02 accepted and merged into `main`  
**Implementation environment:** ChatGPT Work  
**Implementation model:** GPT-5.6 Sol, Extra High reasoning

---

# 1. ROLE

You are the implementation engine for the Between Us Platform.

The MASTER specification and this milestone package are authoritative.

Do not make product, architecture, route, data, framework, backend, or scope decisions outside this package.

Inspect first. Implement second. Validate third. Report last.

M03 is a visual and public-language migration. It requires automated validation and owner visual acceptance before merge.

---

# 2. PROJECT CONTEXT

The repository is a working static catalog originally presented as **QR Jewelry Catalog**.

M01 established the migration safety baseline.

M02 established:

- `window.BETWEEN_US_FINDS` as the normalized source;
- `window.JEWELRY_ITEMS` as the derived legacy adapter;
- permanent public IDs `BU-0001` through `BU-0005`;
- stable slugs;
- lookup helpers;
- 51 passing tests;
- exact preservation of legacy routes, QR behavior, sharing, and current visible behavior.

M03 changes the visible brand and public shell from a jewelry-specific catalog into **Between Us Finds**.

M03 must not change the data model, public routes, QR payloads, reservation workflow, or deployment architecture.

---

# 3. MILESTONE OBJECTIVE

Create the first complete public-facing Between Us shell while preserving all current catalog and item-detail functionality.

M03 must:

1. Replace visible QR Jewelry Catalog branding with Between Us.
2. Implement the approved brand mark as a local SVG asset.
3. Apply the approved palette and typography through reusable CSS tokens.
4. Introduce a responsive shared public header and footer on both pages.
5. Introduce the approved Home, Collections preview, Explore, and About structure.
6. Change visible public vocabulary from products/items to Finds where appropriate.
7. Preserve every current numeric item URL.
8. Preserve every current QR and share URL.
9. Preserve all five current records and existing behavior.
10. Add automated brand/public-shell contracts.
11. Produce a visually reviewable implementation candidate.
12. Stop before push or merge and wait for owner visual acceptance.

---

# 4. PRECONDITIONS

Before making changes:

1. Read:

```text
docs/MASTER_SPECIFICATION.md
docs/MILESTONES/M03.md
docs/BASELINE_BEHAVIOR.md
docs/DOMAIN_MODEL.md
docs/COMPATIBILITY_ADAPTER.md
docs/IDENTIFIER_REGISTRY.md
docs/REPORTS/M01_REPORT.md
docs/REPORTS/M02_REPORT.md
docs/VALIDATION.md
index.html
item.html
app.js
item.js
styles.css
data/items.js
scripts/validate-baseline.mjs
all existing tests
```

2. Run:

```bash
git status --short
git branch --show-current
git fetch origin
git rev-parse main
git rev-parse origin/main
node scripts/validate-baseline.mjs
```

3. Confirm:

- the working tree is clean;
- the current branch is `main`;
- `main` and `origin/main` are synchronized;
- all M01 and M02 validation passes.

4. If any condition fails, stop and report.

5. Create:

```bash
git switch -c migration/m03-between-us-shell
```

---

# 5. APPROVED BRAND CONTRACT

## 5.1 Public names

Primary brand:

```text
Between Us
```

Catalog identity:

```text
Between Us Finds
```

Primary tagline:

```text
Hidden Gems. Honest Prices.
```

Supporting statement:

```text
Discover Something Worth Keeping.
```

## 5.2 Brand personality

The interface must feel:

- discreet;
- elegant;
- local;
- trustworthy;
- curated;
- spacious;
- affordable without looking cheap;
- modern with a subtle vintage character.

Avoid:

- garage-sale styling;
- crowded marketplace layouts;
- loud discount graphics;
- shopping-cart imagery;
- product-category icons;
- hearts;
- hands;
- treasure chests;
- keyholes;
- emoji.

## 5.3 Approved color tokens

Create and use CSS custom properties equivalent to:

```css
--color-charcoal: #252525;
--color-ivory: #F6F1E7;
--color-ivory-soft: #FBF8F2;
--color-olive: #6F7652;
--color-terracotta: #C66F49;
--color-gold: #C79A43;
--color-muted: #6C675F;
--color-border: #D9D0C2;
--color-white: #FFFFFF;
```

Minor contrast adjustments are allowed only when required for WCAG-readable text.

## 5.4 Typography

Use system-available fonts only.

Approved stacks:

```css
--font-display: Georgia, "Times New Roman", serif;
--font-body: Arial, Helvetica, sans-serif;
```

Do not load external web fonts.

## 5.5 Logo mark

Create:

```text
assets/brand/between-us-mark.svg
```

The mark must contain:

- a perfectly round circle;
- a refined `BU` serif monogram;
- a small, discreet four-point gold sparkle;
- restrained charcoal, olive, terracotta, and gold details;
- a transparent background;
- no heart;
- no hands;
- no object icon;
- no keyhole;
- no chest;
- no text other than `BU`.

Requirements:

- local asset;
- scalable SVG;
- accessible `<title>`;
- valid `viewBox`;
- visually clear at 32px and 160px;
- no embedded raster image;
- no external font or asset dependency.

The logo mark must be used in the public header and as the favicon where browser support permits.

---

# 6. PUBLIC LANGUAGE CONTRACT

Replace visible public language where appropriate:

| Existing concept | Public M03 language |
|---|---|
| Jewelry Catalog | Between Us Finds |
| Product / Item | Find |
| Products / Items | Finds |
| Related Products / Related Items | Related Finds |
| View details | View Find |
| Product not found / Item not found | Find not found |
| Back to catalog | Back to Explore |
| Browse jewelry | Explore Finds |

Do not rename internal variables, globals, file names, routes, or compatibility fields merely for branding.

The following internal compatibility names remain approved:

```text
window.JEWELRY_ITEMS
item.html?id=N
data/items.js
```

---

# 7. HOME PAGE PUBLIC SHELL

Refactor `index.html` into a branded single-page public shell while retaining the existing catalog grid and renderer.

## 7.1 Required structure

Use semantic landmarks and stable anchors:

```text
<header>
  shared brand header and navigation
</header>

<main>
  #home          brand hero
  #collections   collections preview
  #explore       current catalog grid
  #about         Between Us explanation
</main>

<footer>
  shared brand footer
</footer>
```

## 7.2 Header

Required:

- logo mark;
- visible `Between Us`;
- optional small `Finds` descriptor;
- navigation:
  - Home
  - Collections
  - Explore
  - About
- mobile-accessible navigation;
- visible keyboard focus;
- no JavaScript-required navigation unless necessary;
- no Reserve by Message action yet.

## 7.3 Hero

Required visible copy:

```text
Between Us Finds
Hidden Gems. Honest Prices.
Discover Something Worth Keeping.
```

Required primary action:

```text
Explore Finds
```

The action must navigate to `#explore`.

Optional secondary action:

```text
Explore Collections
```

The action may navigate to `#collections`.

The hero must not depend on a decorative stock image.

## 7.4 Collections preview

Present these initial collection names:

- Jewelry
- Vintage
- Home & Decor
- Kitchen
- Collectibles
- New Items

Current implementation state:

- Jewelry is the active current collection.
- All other collection cards must be clearly marked `Coming Soon`.
- Do not implement filtering or collection routes.
- Do not make unavailable collection cards behave like active links.
- Do not use category object icons.

Use typography, borders, color, and spacing rather than product icons.

## 7.5 Explore section

Retain the existing catalog grid and current five records.

Required heading:

```text
Explore Finds
```

Required supporting copy may explain that the collection changes over time.

The existing catalog renderer may continue to target its current DOM anchor if that minimizes risk.

## 7.6 About section

Required concise message:

```text
Between Us is a curated local catalog of useful, distinctive, and well-priced finds selected for our community.
```

It may also state:

```text
Each Find is available locally and inventory changes over time.
```

Do not mention seller operations, storage, garage sales, future marketplaces, or internal project history.

## 7.7 Footer

Required:

- Between Us name;
- primary tagline;
- concise community-oriented sentence;
- current year generated without adding a dependency, or a neutral copyright line;
- no social links unless already verified;
- no invented contact information.

---

# 8. FIND DETAIL PUBLIC SHELL

Refactor `item.html` and approved presentation strings in `item.js` to use the same Between Us shell.

Required:

- same public header and footer;
- logo mark;
- link back to `index.html#explore`;
- visible language uses Find / Finds;
- page title updates dynamically to:

```text
{Find title} | Between Us Finds
```

- Related section heading:

```text
Related Finds
```

- QR and share behavior remain unchanged;
- current item data remains unchanged;
- availability behavior remains unchanged;
- image fallback remains unchanged;
- numeric `?id=N` routing remains unchanged.

Do not implement reservation messaging in M03.

---

# 9. VISUAL SYSTEM REQUIREMENTS

## 9.1 Layout

- Mobile-first.
- Preserve current usable breakpoints.
- Maximum readable content width.
- Generous whitespace.
- Cards remain clear and scan-friendly.
- Price and availability remain easy to find.
- Header must not obscure anchor targets.
- Footer must not dominate the page.

## 9.2 Card treatment

Current Find cards must:

- preserve image behavior;
- preserve title;
- preserve price;
- preserve description;
- preserve availability;
- preserve detail link;
- adopt Between Us typography and color system;
- use `View Find` as the action label.

Do not change item ordering.

## 9.3 Accessibility

Required:

- semantic header, nav, main, sections, footer;
- one clear page-level `h1`;
- logical heading hierarchy;
- visible `:focus-visible`;
- sufficient text contrast;
- decorative SVG parts hidden from assistive technology where appropriate;
- logo has an accessible label;
- navigation is keyboard usable;
- no color-only availability communication;
- current image alt/fallback behavior preserved;
- prefers-reduced-motion respected if any motion is introduced.

Avoid unnecessary animation.

---

# 10. IN SCOPE

Expected changes may include:

```text
index.html
item.html
app.js
item.js
styles.css
README.md
scripts/validate-baseline.mjs
scripts/lib/baseline-contracts.mjs
docs/VALIDATION.md
```

Expected additions:

```text
assets/brand/between-us-mark.svg
docs/BRAND_SYSTEM.md
docs/PUBLIC_SHELL.md
docs/REPORTS/M03_REPORT.md
tests/brand/brand-assets.test.mjs
tests/brand/public-shell.test.mjs
```

The exact test structure may vary if the same contracts are covered clearly.

---

# 11. PROTECTED FILES

Do not modify:

```text
data/items.js
tests/fixtures/legacy-items.snapshot.json
docs/IDENTIFIER_REGISTRY.md
assets/images/*
.github/workflows/baseline-validation.yml
```

Do not change:

- public IDs;
- legacy IDs;
- slugs;
- titles;
- prices;
- descriptions;
- availability;
- relationships;
- image paths;
- QR payload logic;
- route format.

If one of these appears necessary, stop and request MASTER approval.

---

# 12. OUT OF SCOPE

Do not implement:

- collection filtering;
- collection routes;
- Featured Finds logic;
- Latest Finds logic;
- Weekly Finds logic;
- Reserve by Message;
- contact information;
- WhatsApp integration;
- new item records;
- new product images;
- new public routes;
- slug routing;
- public-ID routing;
- redirects;
- seller dashboard;
- authentication;
- backend;
- database;
- API;
- payment;
- shipping;
- analytics;
- external fonts;
- UI framework;
- CSS framework;
- package manager;
- third-party dependency;
- QR-library replacement;
- repository-folder rename;
- GitHub-repository rename;
- deployment redesign.

---

# 13. AUTOMATED CONTRACTS

Add dependency-free tests verifying at minimum:

## 13.1 Brand asset

- SVG exists;
- valid SVG root and `viewBox`;
- accessible title exists;
- contains a circular form;
- contains BU monogram text or vector equivalent;
- contains a four-point sparkle;
- contains no raster embedding;
- contains no external references.

## 13.2 Home shell

- page title contains Between Us;
- meta description reflects the brand;
- header, nav, main, and footer exist;
- required anchors exist;
- required navigation labels exist;
- hero copy exists;
- required collection names exist;
- non-Jewelry collections are marked Coming Soon;
- Explore Finds section retains the catalog anchor;
- About copy exists;
- old visible Jewelry Catalog branding is absent.

## 13.3 Detail shell

- header and footer exist;
- back-to-Explore link exists;
- Related Finds public label exists through template or runtime contract;
- item title remains dynamically rendered;
- QR script order remains valid;
- current DOM anchors required by `item.js` remain.

## 13.4 CSS system

- approved CSS custom properties exist;
- approved font stacks exist;
- focus-visible styling exists;
- responsive rules remain;
- no external font import exists.

## 13.5 Compatibility

All M01 and M02 tests must continue to pass.

Update baseline assertions only where M03 intentionally changes public copy or semantic shell.

Do not weaken route, QR, data, or behavior contracts.

---

# 14. VISUAL VALIDATION

Before completion:

1. Start a local static server.
2. Render and inspect:
   - desktop home;
   - mobile home;
   - desktop Find detail;
   - mobile Find detail;
   - invalid Find state;
   - missing-image fallback;
   - available, reserved, and sold states.
3. Confirm:
   - no horizontal scrolling at mobile width;
   - header is usable;
   - navigation anchors work;
   - catalog cards remain readable;
   - detail QR section remains usable;
   - no new console errors;
   - no broken brand asset;
   - all five legacy URLs still work.
4. If the environment supports screenshots, create:
   - `docs/REPORTS/M03_HOME_DESKTOP.png`
   - `docs/REPORTS/M03_HOME_MOBILE.png`
   - `docs/REPORTS/M03_DETAIL_DESKTOP.png`
   - `docs/REPORTS/M03_DETAIL_MOBILE.png`

Screenshots are recommended but not required if the environment cannot produce them reliably. Report whether they were created.

M03 remains pending owner visual acceptance even if Work's visual review passes.

---

# 15. DOCUMENTATION

Create:

## `docs/BRAND_SYSTEM.md`

Document:

- names;
- tagline;
- supporting statement;
- palette with hex values;
- typography;
- logo construction;
- spacing and usage principles;
- prohibited symbols and treatments;
- accessibility rules.

## `docs/PUBLIC_SHELL.md`

Document:

- page structure;
- anchors;
- navigation;
- hero;
- collections preview behavior;
- Explore section;
- About section;
- detail-page shell;
- deferred M04 and M05 functionality.

## `docs/REPORTS/M03_REPORT.md`

Use the report format in Section 18.

Update `README.md` so the repository introduction reflects Between Us Platform while preserving a concise note that the project evolved from the QR Jewelry Catalog MVP.

Do not rewrite historical milestone documents.

---

# 16. VALIDATION COMMANDS

Before completion, run at minimum:

```bash
node --check app.js
node --check item.js
node --check data/items.js
node --test tests/baseline/*.test.mjs
node --test tests/domain/*.test.mjs
node --test tests/brand/*.test.mjs
node scripts/validate-baseline.mjs
git diff --check
git diff --cached --check
git status --short
```

Also run the local visual validation in Section 14.

---

# 17. ACCEPTANCE CRITERIA

M03 implementation candidate is complete only when:

1. Between Us is the visible public brand.
2. Between Us Finds is the visible catalog identity.
3. The approved tagline and supporting statement appear.
4. The approved local SVG mark exists and is used.
5. The approved palette is implemented through CSS variables.
6. No external font or new dependency is added.
7. Home, Collections, Explore, and About sections exist.
8. Collections preview accurately marks future collections Coming Soon.
9. Current five Finds remain visible in their original order.
10. Current data values remain unchanged.
11. Find-detail behavior remains functional.
12. Legacy `item.html?id=N` routes remain functional.
13. Current QR/share behavior remains unchanged.
14. Availability and missing-image behavior remain unchanged.
15. Public vocabulary uses Finds where approved.
16. Reservation messaging is not implemented.
17. All baseline, domain, and brand tests pass.
18. Visual validation passes in desktop and mobile views.
19. No protected file changes.
20. Documentation is complete.
21. One clear milestone commit exists.
22. Working tree is clean.
23. Nothing is pushed or merged.
24. Final status explicitly states:

```text
Implementation candidate — pending owner visual acceptance
```

---

# 18. REQUIRED FINAL REPORT FORMAT

Return a concise report using exactly these sections:

## M03 Result

- Status: PASS, PARTIAL, or BLOCKED
- Acceptance state: `Implementation candidate — pending owner visual acceptance`
- Branch
- Commit
- Base commit
- Rollback reference

## Repository inspected

- Important files and M01/M02 safeguards confirmed

## Brand implementation

- Public name
- Catalog identity
- Tagline
- Supporting statement
- Logo asset
- Palette
- Typography

## Public shell implemented

- Home
- Navigation
- Collections preview
- Explore
- About
- Footer
- Find detail shell

## Files created

- Exact paths

## Files modified

- Exact paths
- State explicitly whether any protected file changed

## Files removed

- Exact paths or `None`

## Validation executed

- Exact commands
- PASS/FAIL
- Total tests

## Visual validation

- Desktop home
- Mobile home
- Desktop detail
- Mobile detail
- Invalid Find
- Missing image
- Availability states
- Console
- Screenshots created or not created

## Compatibility results

- Legacy routes
- Numeric IDs
- Data parity
- Related Finds
- QR/share
- Image fallback
- Responsive behavior

## Warnings and known limitations

- Coming Soon collections
- Deferred M04 discovery behavior
- Deferred M05 reservation behavior
- External QR dependency
- Any remaining visual concerns

## Git status

- Final branch
- Final commit hash
- Working-tree state
- Push status

## Owner visual-review instructions

Provide:

- exact local preview command;
- exact home URL;
- exact detail URL for one real-image Find;
- exact detail URL for one missing-image Find;
- what the owner should inspect.

## Recommended next step

The expected recommendation is:

```text
Owner visual review and acceptance of M03.
```

Do not begin M04.

---

# 19. GIT REQUIREMENTS

After validation passes:

```bash
git add .
git commit -m "feat: add Between Us public shell"
git status --short
```

Requirements:

- do not push;
- do not merge;
- do not create a remote tag;
- leave the working tree clean;
- report the final commit;
- report the base commit as rollback reference.

---

# 20. STOP CONDITIONS

Stop and report without destructive changes if:

- the repository is not clean;
- `main` differs from `origin/main`;
- M01 or M02 validation fails;
- the M03 package is missing;
- a protected data, identifier, image, workflow, or fixture file appears to require modification;
- route, QR, or share compatibility cannot be preserved;
- a new dependency appears necessary;
- exact approved public copy is ambiguous;
- the logo cannot be implemented locally as SVG;
- visual validation reveals a blocking regression.

Wait for MASTER review after submitting the M03 report.
